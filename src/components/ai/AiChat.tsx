// TODO: Interface de chat com IA — envia dados frescos do banco + mensagem do usuário
// Usa Vercel AI SDK (streamText) com Claude Sonnet 4.5
// Princípio: a IA nunca "lembra" — sempre recebe dados frescos antes de responder
export function AiChat() { return null; }
